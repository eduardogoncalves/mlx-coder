// Tests/IntegrationTests/PlaceholderTests.swift
// Placeholder so the test target has at least one file

import XCTest

final class PlaceholderTests: XCTestCase {
    func testPlaceholder() {
        // Integration tests require the model to be downloaded.
        // This placeholder ensures the test target compiles.
        XCTAssertTrue(true)
    }
}
