name: SonarCloud

on:
  push:
    branches: ["main"]
  pull_request:
    branches: ["main"]

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

jobs:
  sonarcloud:
    name: SonarCloud Analysis
    runs-on: macos-latest
    permissions:
      contents: read

    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - name: Build and collect coverage
        run: |
          swift test --enable-code-coverage 2>&1 || true
          xcrun llvm-cov export \
            .build/debug/mlx-coderPackageTests.xctest/Contents/MacOS/mlx-coderPackageTests \
            --instr-profile .build/debug/codecov/default.profdata \
            --format=lcov \
            --ignore-filename-regex="(.build|Tests)" \
            > coverage.lcov || true

      - name: SonarCloud Scan
        if: ${{ secrets.SONAR_TOKEN != '' }}
        uses: SonarSource/sonarqube-scan-action@55e44800a8f495208cce6e4e82f5dedb45fcf0ef # v7.2.0
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
